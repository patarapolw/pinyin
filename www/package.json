{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base /pinyin/",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.0",
    "vite": "^2.3.5"
  },
  "dependencies": {
    "chinese-to-pinyin": "^1.3.1"
  }
}
