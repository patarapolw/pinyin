{
  "version": "0.0.0",
  "scripts": {
    "dev": "snowpack dev",
    "build": "snowpack build",
    "predeploy": "snowpack build",
    "deploy": "touch dist/.nojekyll && gh-pages -t -d dist"
  },
  "dependencies": {
    "chinese-to-pinyin": "^1.3.1"
  },
  "devDependencies": {
    "gh-pages": "^3.2.0",
    "snowpack": "^3.5.4"
  }
}
